package com.enterprise.rental.dao;

import com.enterprise.rental.dao.mapper.UserMapper;
import com.enterprise.rental.entity.UserDto;

import java.util.List;

import static java.util.stream.Collectors.toList;

public class UserFacade {
//    private UserDao userDao;
//    private UserMapper mapper;
//
//    public List<UserDto> getAllUsers() {
//        return userDao.findAll().stream().map(mapper::toDto).collect(toList());
////        return userRepo.findAll().stream().map(this::toDto).collect(toList());
//    }
}
